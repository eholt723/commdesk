services:
  server:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "3001:7860"
    environment:
      - NODE_ENV=production
      - PORT=7860
      - DATABASE_URL=${DATABASE_URL}
    restart: unless-stopped
